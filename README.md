# express-mongodb-react-redux-products-grid
Products grid with filtering by categories &amp;&amp; CRUD

For setup please run following commands:
`cd server && npm i && cd ..`
`cd client && npm i && cd ..`

then run 
`npm start`
